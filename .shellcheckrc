# ShellCheck configuration for Flipper BlackHat project
# https://github.com/koalaman/shellcheck/wiki/Directive

# Enable all optional checks
enable=all

# Disable specific checks that don't apply to this project

# SC2034: Variable appears unused
# Many variables are used in sourced files or by external tools
disable=SC2034

# SC2086: Double quote to prevent globbing and word splitting
# We intentionally use unquoted variables in some array contexts
# Disabled globally, but should be reviewed case-by-case
# disable=SC2086

# SC1090: Can't follow non-constant source
# We source files dynamically, which is intentional
disable=SC1090

# SC1091: Not following sourced file
# External files may not be available during linting
disable=SC1091

# SC2155: Declare and assign separately to avoid masking return values
# We accept this trade-off for cleaner code
disable=SC2155

# Severity threshold - show warnings and above
severity=style

# Shell dialect - bash specific features allowed
shell=bash

# Source path for sourced files (relative to project root)
source-path=SCRIPTDIR
